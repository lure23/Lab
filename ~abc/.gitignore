/node_modules/
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Cloudflare
/.wrangler/
.dev.vars

# tbd. When going to production, we might want to use 'package-lock.json' (in the version control!) to make sure
#   the versions are the same as in dev.
#/package-lock.json
